<!DOCTYPE html>
<html>
  <head>
    <title>Hangover</title>
    <%= favicon_link_tag "favicon.png", rel: "icon", type: "image/png" %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.js"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class=<%= "bg-dark-grey" if controller_path == "participants"%>>
    <%= render "shared/navbar" unless controller_path == "participants" && controller_path == "chatrooms" && controller_path == "parties" %>
    <%= render "shared/flashes" %>
    <%= yield %>

      <%= render "shared/bottom_nav" unless (controller_name == "parties") || (controller_name == "chatrooms" && action_name == "show") || (controller_name == "participants") %>

  </body>
</html>
