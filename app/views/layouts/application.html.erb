<!DOCTYPE html>
<html>
  <head>
    <title>Hangover</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.js"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>

    <!-- Facebook Open Graph data -->
    <meta property="og:title" content="Hangover" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="www.hangover.lol" />
    <meta property="og:image" content="cover.png" />
    <meta property="og:description" content="This is the best webapp to go out with your friends and have fun" />
    <meta property="og:site_name" content="Hangover - Go out and have fun" />

  </head>
  <body class=<%= "bg-dark-grey" if controller_path == "participants"%>>
    <%= render "shared/navbar" unless (controller_name == "chatrooms" && action_name == "show") || (controller_name == "participants") || (controller_name == "pages" && action_name == "welcome")%>
    <%= render "shared/flashes" %>
    <%= yield %>

      <%= render "shared/bottom_nav" unless (controller_name == "parties") || (controller_name == "invitations") || (controller_name == "chatrooms" && action_name == "show") || (controller_name == "participants") || (controller_name == "pages" && action_name == "welcome") %>

  </body>
</html>
