<div>
  <div>
  <%= form_with url: parties_path, class: "search-form-control form-group", method: :post do |f| %>
    <%= f.text_field :address, placeholder: "Enter Location...", class: "form-control string required" %>
  <% end %>
  </div>
  <div>
    <div class="map-container">
      <div style="width: 100%; height: 115vh;"
        data-controller="map"
        data-map-markers-value="<%= @markers.to_json %>"
        data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
  </div>

  <%# <%= link_to "Confimre Location", new_party_participant_path, class:"btn-search"%>
</div>
