<div>
  <div>
  <%= form_for @party, class: "search-form-control form-group-hangover" do |f| %>
    <%= f.text_field :address, placeholder: "Enter Location..." %>
  <% end %>
  </div>
  <div>
    <div class="map-container">
      <div style="width: 100%; height: 115vh;"
        data-controller="map"
        data-map-markers-value="<%= @markers.to_json %>"
        data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
  </div>

  <%# <%= link_to "Confimre Location", new_party_participant_path, class:"btn-search"%>
</div>
